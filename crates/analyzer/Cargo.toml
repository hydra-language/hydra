[package]
name = "analyzer"
version = "0.1.0"
edition = "2021"

[dependencies]
parser = { path = "../parser" }
ir = { path = "../ir" }
lexer = { path = "../lexer" }   
errors = { path = "../errors" }

[lib]
name = "analyzer"
path = "src/mod.rs"
